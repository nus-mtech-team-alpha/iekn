name: 00 - Main CI / CD Pipeline

on: 
  push:
    branches: [ "development" ]
    paths-ignore: 
    - '**/*.md'
    - '**/*.txt'
  pull_request:
    branches: [ "development" ]
    types: [opened, synchronize, reopened]
    paths-ignore: 
      - '**/*.md'
      - '**/*.txt'
  workflow_dispatch: